[package]
name = "ssterm_terminal_core"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
