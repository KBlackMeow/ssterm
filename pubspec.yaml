name: ssterm
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  xterm:
    path: packages/xterm
  flutter_pty:
    path: packages/flutter_pty
  dartssh2: ^2.17.1
  crypto: ^3.0.6
  pointycastle: ^4.0.0
  path: ^1.9.1
  desktop_drop: ^0.4.4
  flutter_secure_storage: ^10.2.0
  package_info_plus: ^10.1.0
  ffi: ^2.1.0
  window_manager: ^0.4.3
  file_picker: ^12.0.0-beta.5
  share_plus: ^13.0.0
  gpt_markdown: ^1.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: any
  flutter_launcher_icons: ^0.14.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

flutter_launcher_icons:
  ios: true
  android: true
  image_path: "assets/icon/icon_ios.png"
  remove_alpha_ios: true
  adaptive_icon_background: "#121E33"
  adaptive_icon_foreground: "assets/icon/icon_ios.png"
  # Windows .ico is generated by tool/gen_win_icon.dart (multi-resolution,
  # 16-256px). flutter_launcher_icons only emits a single 256px frame, which
  # would look soft at title-bar size, so Windows is intentionally omitted here.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/scripts/cmd.json

  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Light.ttf
          weight: 300
        - asset: assets/fonts/JetBrainsMono-LightItalic.ttf
          weight: 300
          style: italic
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
          weight: 400
        - asset: assets/fonts/JetBrainsMono-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/JetBrainsMono-Bold.ttf
          weight: 700
        - asset: assets/fonts/JetBrainsMono-BoldItalic.ttf
          weight: 700
          style: italic
    - family: MonacoBundled
      fonts:
        - asset: assets/fonts/Monaco.ttf
    - family: SFMonoPowerline
      fonts:
        - asset: assets/fonts/SF-Mono-Powerline-Light.otf
          weight: 300
        - asset: assets/fonts/SF-Mono-Powerline-Light-Italic.otf
          weight: 300
          style: italic
        - asset: assets/fonts/SF-Mono-Powerline-Regular.otf
          weight: 400
        - asset: assets/fonts/SF-Mono-Powerline-Regular-Italic.otf
          weight: 400
          style: italic
        - asset: assets/fonts/SF-Mono-Powerline-Medium.otf
          weight: 500
        - asset: assets/fonts/SF-Mono-Powerline-Medium-Italic.otf
          weight: 500
          style: italic
        - asset: assets/fonts/SF-Mono-Powerline-Semibold.otf
          weight: 600
        - asset: assets/fonts/SF-Mono-Powerline-Semibold-Italic.otf
          weight: 600
          style: italic
        - asset: assets/fonts/SF-Mono-Powerline-Bold.otf
          weight: 700
        - asset: assets/fonts/SF-Mono-Powerline-Bold-Italic.otf
          weight: 700
          style: italic
        - asset: assets/fonts/SF-Mono-Powerline-Heavy.otf
          weight: 800
        - asset: assets/fonts/SF-Mono-Powerline-Heavy-Italic.otf
          weight: 800
          style: italic
