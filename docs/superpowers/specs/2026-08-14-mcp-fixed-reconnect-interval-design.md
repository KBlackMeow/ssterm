# MCP 固定重连间隔设计

## 目标

让已启用 MCP 服务在断线后以恒定的 3 秒间隔尝试重连。

## 决策

- `McpReconnectBackoff.delayForFailure` 对任意失败次数都返回 `Duration(seconds: 3)`。
- 保留现有的单个重连计时器保护，避免同一服务重复排队重连。
- 成功连接时清空失败次数；手动断开、禁用和关闭时取消计时器，保持现有行为。

## 验证

- 单元测试覆盖首次与重复失败均返回 3 秒。
- 运行 MCP 重连测试、静态分析和完整 Flutter 测试套件。
